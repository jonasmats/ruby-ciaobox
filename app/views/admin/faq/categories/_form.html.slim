.portlet.light.bg-inverse
  .portlet-body.form
    = form_for [:admin, @category], html: {class: "form-horizontal"} do |f|
      = render "/layouts/shared/form_errors/admin", object: f.object
      .form-body
        .form-group.last
          label.col-md-3.control-label
            = Faq::Category.h :name
          .col-md-4
            = f.text_field :name, class: "form-control", placeholder: "#{t('.placeholder.name')}"
      .form-actions
        .row
          .col-md-offset-3.col-md-4
            = f.submit class: "btn green"
            = link_to admin_faq_categories_path, class: "btn default" do
              = t(".cancel")